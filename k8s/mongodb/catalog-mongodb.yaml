apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog-mongodb-pvc
  namespace: cake-delight
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-mongodb
  namespace: cake-delight
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-mongodb
  template:
    metadata:
      labels:
        app: catalog-mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:8
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: catalog-mongodb-storage
              mountPath: /data/db
      volumes:
        - name: catalog-mongodb-storage
          persistentVolumeClaim:
            claimName: catalog-mongodb-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: catalog-mongodb
  namespace: cake-delight
spec:
  selector:
    app: catalog-mongodb
  ports:
    - port: 27017
      targetPort: 27017

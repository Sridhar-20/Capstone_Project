apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notification-mongodb-pvc
  namespace: cake-delight
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-mongodb
  namespace: cake-delight
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-mongodb
  template:
    metadata:
      labels:
        app: notification-mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:8
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: notification-mongodb-storage
              mountPath: /data/db
      volumes:
        - name: notification-mongodb-storage
          persistentVolumeClaim:
            claimName: notification-mongodb-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: notification-mongodb
  namespace: cake-delight
spec:
  selector:
    app: notification-mongodb
  ports:
    - port: 27017
      targetPort: 27017

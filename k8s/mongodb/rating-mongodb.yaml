apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rating-mongodb-pvc
  namespace: cake-delight
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-mongodb
  namespace: cake-delight
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rating-mongodb
  template:
    metadata:
      labels:
        app: rating-mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:8
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: rating-mongodb-storage
              mountPath: /data/db
      volumes:
        - name: rating-mongodb-storage
          persistentVolumeClaim:
            claimName: rating-mongodb-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: rating-mongodb
  namespace: cake-delight
spec:
  selector:
    app: rating-mongodb
  ports:
    - port: 27017
      targetPort: 27017
